document.addEventListener('DOMContentLoaded',()=>{
    //PRODUCTOS CARGAR EN HTML
    cargarClientes();
    cargarFormularioClientes();
    //PRODUCTOS CARGAR EN HTML
    cargarProductos();
    cargarFormularioProductos();
    // FACTURAS CARGAR EN HTML
    subirFacturas();
    cargarFormularioFacturas();
});